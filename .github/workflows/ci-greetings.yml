name: CI Greetings Pipeline

on:
  push:
    branches:
      - main

jobs:
  greetings:
    runs-on: ubuntu-latest

    steps:
      - name: Print greeting message
        run: echo "Hello! Welcome to GitHub Actions 🚀"
